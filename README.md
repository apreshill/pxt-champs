# pxt-champs

A scratch repo for running the Pixeltable Quickstart as a notebook.

This repo uses Pixeltable 0.7.3 from PyPI. Open `quickstart.ipynb` from this folder. `uv add 'pixeltable[serve]'` installs it. After that, every command is `uv run pxt ...`. No pip.
